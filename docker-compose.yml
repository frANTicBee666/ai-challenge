version: "3.9"
services:
  web:
    build: .
    container_name: yandexgpt-chat
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - YANDEX_API_KEY=${YANDEX_API_KEY}
      - YANDEX_FOLDER_ID=${YANDEX_FOLDER_ID}
    restart: unless-stopped


